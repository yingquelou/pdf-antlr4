#include "binStream.h"
namespace antlr4
{
    void binStream::consume()
    {
        // TODO:
    }

    size_t binStream::LA(ssize_t i)
    {
        return size_t();
    }
    ssize_t binStream::mark()
    {
        return ssize_t();
    }
    void binStream::release(ssize_t marker)
    {
    }
    size_t binStream::index()
    {
        return size_t();
    }
    void binStream::seek(size_t index)
    {
    }
    size_t binStream::size()
    {
        return size_t();
    }
    std::string binStream::getSourceName() const
    {
        return std::string();
    }
    std::string binStream::getText(const misc::Interval &interval)
    {
        return std::string();
    }
    std::string binStream::toString() const
    {
        return std::string();
    }

} // namespace antlr4
